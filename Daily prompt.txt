You are running Metro Gnome's daily SEO and growth session.

Read seo_context.md and the last two reports in seo_reports/ before doing anything else.

──────────────────────────────────────────
STEP 1 — RESEARCH (you do this, not the user)
──────────────────────────────────────────

Run all of these. Do not skip any.

a) git log --oneline -10
   What shipped since the last report? Any new features worth surfacing in SEO?

b) Web search each competitor in seo_context.md for updates.
   Search: "[app name] android update 2026" or check their Play Store page.
   Only flag genuinely new information — not things already in seo_context.md.

c) Web search for new roundup coverage.
   Search: "best metronome app android [current year]" and "best tuner app android [current year]"
   Any new articles not already in seo_context.md? Is Metro Gnome mentioned anywhere new?

d) Web search for active Reddit threads.
   Search: site:reddit.com "metronome app" OR "tuner app" — last 30 days.
   Any thread where a genuine Metro Gnome mention or reply would add value?

e) Check Open Action Items in seo_context.md.
   What is overdue? What is newly due based on today's date?

──────────────────────────────────────────
STEP 2 — FULL ANALYSIS
──────────────────────────────────────────

Write the day's findings:
- What changed since the last report (app, competitors, web presence)
- Status of open action items — mark anything newly completed or overdue
- ONE recommendation, fully written out. Not described — actually written:
  - If it's an email: write the full email (subject + body)
  - If it's a Reddit post/reply: write the full text
  - If it's a listing change: write the before and after
  - If it's a code/content change: specify file, line, exact change

If nothing material has changed: say so in one sentence. Do not pad.

──────────────────────────────────────────
STEP 3 — DATA REQUEST (only if needed)
──────────────────────────────────────────

Ask the user for data ONLY if your research identified a specific gap that
real data would resolve. Examples of valid asks:
- "The Speed Trainer blog post is live but I can't see whether it's getting
  impressions — fetch GSC data for metrognome.co.za/blog/speed-trainer"
- "I found a new roundup but can't tell if Metro Gnome's rating would qualify —
  paste the current Play Store rating and review count"

Do NOT make a generic rotating data request if no specific gap was found.
Do NOT ask for data you could find yourself via web search.

──────────────────────────────────────────
STEP 4 — SAVE & UPDATE
──────────────────────────────────────────

Save report to seo_reports/YYYY-MM-DD.md.

Update seo_context.md:
- Mark completed action items as done (with date)
- Add any newly discovered outreach targets, competitors, or keyword findings
- Remove stale entries (old action items that are done or cancelled)
- Keep it accurate — it is the single source of truth read at the start of every session
