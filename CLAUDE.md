# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Build Commands

```bash
./gradlew assembleDebug           # Debug APK
./gradlew assembleRelease         # Release APK (requires keystore.properties + metrognome-release.jks)
./gradlew test                    # Unit tests
./gradlew connectedAndroidTest    # Instrumented tests (requires connected device/emulator)
./gradlew lint                    # Static analysis
./gradlew clean                   # Clean build outputs
```

## Architecture Overview

**MetroGnome** is an Android metronome app with a rhythm game. Single-module Kotlin/Compose project targeting SDK 37, min SDK 24.

**Package vs applicationId, a recurring trip-up:** source lives under `com.example.metrognome` (the `namespace`), while the published `applicationId` is `com.wynandl.metrognome`. Paths are therefore `app/src/main/java/com/example/metrognome/...`, so a `find` or `grep` against `com/wynandl/` returns nothing and looks like the file is missing. The Play Store id, Firebase config and all external references use `com.wynandl.metrognome`.

### Layer Structure

```
UI (Jetpack Compose screens + Canvas)
    ↓
ViewModels (AndroidViewModel + StateFlow)
    ↓
Audio Engine + Game Logic (Coroutines on Dispatchers.Default)
```

### Key Components

**Audio engine layout** — each engine lives in its own sub-package under `audio/`:

- `audio/metronome/MetronomeEngine.kt` — Raw `AudioTrack` in STREAM mode for sample-accurate timing. Pre-generates a click voice for every sound type, keyed by `soundType` index: free (0 Classic, 1 Hi-Hat, 2 Wood, 3 Warm) and premium (4 Bell, 5 Crystal Bowl, 6 Kalimba, 7 Cowbell, 8 Metal Kick), each a synthesized buffer. **A voice has to survive a phone speaker**, which puts out almost nothing below ~300 Hz, and for the Metal Kick (added 2026-09-21, after a Megadeth drum; the dev's brief in the end was "a thump, a doof-doof, think Pantera and Slipknot") that pulls directly against what the voice is. Four cuts settled it: a big gated room read as reverb; a click-forward cut read as "soft and boring"; a pure-sine sub set the peak while being inaudible; and the shipped cut is a **sustained, saturated low body** (160 to 58 Hz over 60 ms, a 220 ms decay, driven almost to a square wave by an asymmetric `tanh`) with a 200 to 80 Hz punch, a beater at less than half the body's level (the dev's last note: "still too clicky, turn the beater down and more body") and a hint of room. A voice this long outlasts the beat above ~140 BPM, and `buildBeatBuffer` used to cut a voice dead at the beat boundary; it now fades the cut over 20 ms, for every voice, or the thump ended mid-swing with a click on each beat. And the hit itself fades to silence over its last 60 ms: an exponential decay never reaches zero, the buffer used to end with the body still at ~13%, and the dev heard every beat end "like an immediate stop, the engine closing". The other voices use `(1 - p)^n` envelopes that reach zero by construction; any exponential envelope needs an explicit fade-out, and `MetalKickTest` now asserts the last 5 ms are silent. The saturation is the trick that lets a thump through a small speaker at all: a square body is louder than a sine at the same peak, and its harmonics at 180, 300 and 420 Hz are what the ear hears the 60 Hz through. `MetalKickTest` reads the engine's private buffers on the JVM and encodes the trade: the hit's 50 ms RMS must be over 40% of its peak (a thump, not a click), and through a simulated phone speaker (two 2nd-order 350 Hz high-pass stages) it must still peak above a quarter of full, the bar the dev chose over the 40% the click-forward cut managed. Each hit normalises to its own `volume` (the mix peaks well over 1.0; normalising both to full scale left the accent no louder than the click), with a 1 ms fade-in. Any new voice gets a test of this shape, with its own bars. Runs a blocking write loop on `Dispatchers.Default`. The `onBeat` callback fires **before** the audio write (not after) so the UI callback arrives ~16ms early, keeping Compose animations in sync with sound.
- `audio/rhythm/RhythmDetector.kt`: Mic input via `AudioRecord`, used by all three mic features. A `ClapDetector` tells the metronome click apart from a clap by signature (the 1100 Hz Classic click is low-band dominant; a clap is broadband) and emits **only claps**, so an on-beat clap still scores and a leaked click never does, with no time-suppression window. AEC (Acoustic Echo Cancellation) is enabled when available; NoiseSuppressor is intentionally **disabled** (it would filter claps). Onset timing is localised inside each buffer and mapped to `elapsedRealtime` via `FrameClock` + `AudioRecord.getTimestamp`. Because the classifier is tuned to the Classic voices, mic mode forces the Classic **sound type** (`MetronomeViewModel.setMicSoundOverride` / `effectiveSoundType`); the engine keeps its normal 1100 Hz click and normal 1800 Hz downbeat accent, and `ClapDetector` rejects **both** narrowly (a dedicated 1800 Hz accent band alongside the 1100 Hz click band), so the accent stays clearly audible to the player yet the detector never misreads it as a clap. (The Rhythm Game uses its own engine and plays uniform clicks.)
- `audio/rhythm/RoomNoiseMonitor.kt`: Passive observer of `RhythmDetector.amplitude` (never touches capture/detection). An asymmetric-EMA noise floor (fast-down, slow-up) tells brief claps from a sustained noisy room; exposes `noisy: StateFlow<Boolean>`, which drives `ui/components/RoomNoiseIndicator.kt` during play. The `floor` EMA behind that decision is **private** and there is no `everNoisy` flag, so nothing here is wired to telemetry.
- `audio/tuner/` — Tuner engine: `Tuner.kt` (MPM + FFT pitch detection), `TunerCalibrator.kt` (loopback/reference calibration), `AmbientDetector.kt` (room profiling, speech rejection), `TunerFeedbackConfig.kt` + `TunerFeedbackReporter.kt` (Firestore diagnostic submissions).
- `audio/drone/` — Tuning drone (sustained reference tone for tuning by ear), four files with one job each: `DroneVoice.kt` (pure data: `DroneTimbre`, `DroneBlend`, and `buildVoice()` compiling them into a `VoiceLayout`), `DroneRenderer.kt` (pure-Kotlin block synthesis, no Android), `DroneEngine.kt` (AudioTrack + coroutine + thread marshalling), `DroneState.kt` (the persisted settings value). The split is what makes the synthesis unit-testable on the JVM, including against the app's own `PitchDetector`.
  - **The centre-strand invariant.** Every timbre is a stack of "strands" (whole harmonic series a few cents apart, panned outward, each breathing on its own sub-Hz LFO so the beating never freezes). Exactly one strand per timbre is undetuned, unmoving and centred, and `DroneVoiceTest` enforces it. That is what lets a tone this alive still be an exact pitch reference; put movement on the centre strand and the drone stops being usable for its only job.
  - **Timbres must be harmonic.** A struck-bowl voice with stretched partials was built, sounded good, and was removed: `DroneRendererTest` caught the app's own tuner reading it 13 cents sharp, because an inharmonic series has no single period to find. Do not reintroduce inharmonic partials here; the metronome's Crystal Bowl click can afford them because nobody tunes to a click.
  - **Blend intervals are just, not tempered** (3:2, 2:1). A beatless just fifth is the cue a player listens for; an equal-tempered fifth would beat against itself. A consequence worth knowing: root + just fifth repeats at half the root frequency, so any period-based detector correctly reports the octave below. That is the missing fundamental, not a bug, which is why the blend tests measure each tone on its own frequency instead of running the mix through `PitchDetector`.
  - **Nothing in the audio path steps.** Start/stop are raised-cosine envelopes, note changes glide in the log-frequency domain, level follows a one-pole, and a timbre or blend change crossfades between two live voices. The crossfade is deliberately **linear**, not equal-power: the two voices share a fundamental and are strongly correlated, so their amplitudes add and a square-root law bulges audibly mid-change. `DroneRendererTest.noTransitionProducesADiscontinuity` measures every transition against the steepest step the steady tone itself takes.
  - **Sub-cent claims are measured, not detected.** `PitchDetector` carries a few cents of uncalibrated bias (that is what the tuner's calibration exists to remove), so it cannot be the yardstick for accuracy finer than that. The renderer tests pin frequency exactly via a phase-difference measurement across two halves of a capture, and use `PitchDetector` separately, at a 5-cent bar, to prove the harmonics and strands do not pull the perceived note off its fundamental.
  - **The note picker is a drawn one-octave keyboard with no letters on it**, which only works because the naturals are light (`AppColors.keyNatural`) and the sharps near-black. The first version drew both in shades of the same dark violet on the theory that the black-key groups of two and three would orient the eye; they cannot orient anything they are not visibly distinct from, and the dev reported it read as an unexplained row of blocks. If a drawn control is carrying meaning instead of a label, the contrast carrying that meaning is load-bearing, not styling.
  - **The panel opens expanded on a first visit**, and `TunerViewModel.droneExpanded` (SharedPreferences `drone_panel_expanded`, defaulting true) then keeps whatever the user last chose. Shipped collapsed first: the timbre, blend and level controls were discoverable only by finding a chevron, and the header's own "F2 . Reed" readout consequently read as a bug, since it named a setting with no visible way to reach it. People do not go exploring, so a feature worth having is worth showing once. `DronePanel` takes `expanded`/`onExpandedChange` as parameters rather than holding the state, since the ViewModel is what persists it.
  - `NoteNames.frequencyOf(midi, referenceHz)` (added with this feature) is the single place a named note becomes a frequency, so the tuner's reference pitch moves the drone's tone by the same arithmetic that moves the needle's target. `DroneState` stores a MIDI number, never Hz.
- `audio/selftest/` — Acoustic-loopback mic self-test ("Groove Check"): `MicSelfTest.kt` plays clicks/claps through the speaker, listens back, and grades the device (GOOD_FIT / USABLE / NOT_FIT). Design principle: the test's job is to *calibrate* the device as far as possible, not to disqualify it - a device fails only when nothing short of magic would fix it, never because a shipped default happened to be wrong for it. Discrimination's hard gates are inseparability (both spectral axes overlap, tolerating a `SEPARABILITY_MIN_FRACTION` minority of outlier claps rather than one flaky sample) and `MIN_CLAP_ONSET_RECALL` (claps not registering as onsets at all, before classification); a low classified click-reject/clap-detect rate on an otherwise-separable device is advisory only (`NoteCode.CLICK_LEAK_CALIBRATED`/`CLAP_MISS_CALIBRATED`), not a failure. The scoring phase reclassifies captured onsets against THIS run's own just-derived per-device thresholds (`deviceClapBandRatio`/`deviceClapFlatnessMin`) rather than the shipped defaults capture ran with all along - so a device is graded on what it will actually do once calibrated, not on its out-of-the-box fit. Small-sample recall bars (`MIN_CLAP_ONSET_RECALL`, `OUT_OF_BAND_MIN_RECALL`) tolerate one miss out of ~12 trials rather than demanding a literally perfect run. On PASS, `SelfTestCalibrationStore` persists the latency constant plus per-device `ClapDetector` thresholds for both acceptance axes (`clap_band_ratio`, `clap_flatness_min`); `MicCalibration` is the single read surface all three mic features use. `SelfTestThresholds.GATE_LOGIC_VERSION` (currently 4) stamps stored verdicts - bump it on any gate change that could flip outcomes (a stale FAIL then reads as never-tested and the check is re-offered; a PASS is never invalidated). Diagnostics are `NoteCode` entries (short id + looked-up text, e.g. `D2`/`S1`), never raw sentences, so Firestore rows stay one line; codes exist for calibration offsets, thin-but-passing margins (`THIN_SEPARATION_MARGIN`), and near-limit passes (`NEAR_VOLUME_FLOOR`/`NEAR_NOISE_CEILING`) as well as hard failures. Outcomes report anonymously to Firestore `mic_self_test` via `cloud/MicCheckReporter`, including spectral margins, the tuned thresholds actually used, and a human-readable SAST timestamp alongside the raw epoch.
- `audio/dsp/` — Shared DSP building blocks used by the engines: `FFT.kt`, `PitchDetector.kt`, `ClapDetector.kt` (spectral click-vs-clap, the only onset path mic mode uses; accepts a clap on flatness OR band-ratio, and both thresholds are per-device-calibratable constructor params fed from `MicCalibration`), `BiquadFilter.kt`.

**Groove Check permission recovery** (added 2026-08-10, shipped v5.17; consolidated 2026-08-14, v5.18/72): `MicCalibration.isActive` is a SharedPreferences-only check (`enabled && isCalibrated`) — it says nothing about whether the OS still actually holds `RECORD_AUDIO`. That gap matters because an uninstall/reinstall restores SharedPreferences via Android's default app-data backup (nothing in this repo excludes them — see `AndroidManifest.xml`'s `dataExtractionRules`/`fullBackupContent`, both pointed at empty rule files) while Android *never* restores runtime permission grants, so `isActive` can read true with the mic actually inaccessible. `ui/components/MicPermissionRecovery.kt` (`rememberMicPermissionRecovery()`) is the single shared source that guards against this, reading `ContextCompat.checkSelfPermission` live rather than trusting the flag — it replaced three separately-duplicated copies of the same permission-launcher block (in `SettingsScreen.kt`, `MetronomeScreen.kt`, and `RhythmGameScreen.kt`) that had drifted apart in a way that let the same underlying bug re-appear in two of the three places even after the first was fixed. It exposes two entry points, used by all three call sites:
- `startWithMicPermissionCheck(start)` — wraps a session-start action: `MetronomeScreen.kt` (Practice's and Speed Trainer's Start actions) and `GameCard` in `RhythmGameScreen.kt` (each difficulty tap). If the flag says on but the live permission is missing, it fires the real permission dialog immediately at the moment of starting and only calls the actual start function once that resolves, so a granted permission takes effect for that same session, not just the next one.
- `fixPermission()` — what `ui/components/MicTimingNudge.kt`'s "needs microphone access · tap to fix" pill (Practice/Speed Trainer/Rhythm Game) and `SettingsScreen.kt`'s Groove Check toggle both call on an already-calibrated device: a lightweight re-request (or a hop to App Settings if permanently denied), deliberately never the full Groove Check — the device already proved itself once, so re-running the whole onboarding flow would be a confusing, needless detour. `MicTimingNudge` shows a dedicated "needs microphone access" state instead of either lying ("Groove Check is on") or going silent when this fix action isn't wired (a non-interactive "Fix in Settings" note instead).
- `audio/NoteNames.kt` — Shared music-theory utility (note name ↔ frequency mapping); stays at the `audio` root because all three engines may reference it.

**Drone / microphone exclusivity** (added 2026-09-03). `TunerViewModel.resumeListening()` is the *only* place `tuner.start()` is called outside the initial entry point. Three separate things can hold the audio path: the screen having navigated away (`listenIntent`), a calibration run, and the drone. Before this there were two independent resume paths (`startListening` and the tail of `runCalibration`) that each knew about a different subset, which is exactly the shape of the bug the `MicPermissionRecovery` consolidation fixed on the permission side. Every path that finishes one of those conditions now calls back into `resumeListening()`, which re-checks all three plus the live permission; no caller has to remember the others and none can re-open the mic behind another's back.

Starting the drone closes the mic, deliberately: the tuner would hear the app's own tone through the speaker and lock onto it, reporting a perfectly in-tune note that is entirely its own output. The needle has nothing to add while a reference tone is sounding, since tuning by ear against it is the whole point. `DronePanel` says so on screen ("Tuner needle paused" / "The needle returns when you stop the drone.", deliberately without the reason: a user needs to know it is intentional and reverses itself, not why), and `AmbientPanel` takes a `listeningPaused` flag so it shows "Paused for the drone" with no state icon lit, rather than sitting on "Getting ready…" as though it were stuck. `runCalibration` stops the drone outright, since loopback calibration measures what comes back from the speaker.

`stopListening()` (fired when the Tuner tab is left) deliberately does **not** stop the drone: a sounding drone carrying over to the metronome tab is the practice mode the two make together, matching how the metronome itself keeps playing across tabs. It ends on an explicit stop or in `onCleared`. `DroneState.playing` is never persisted — the drone is opt-in on every launch, and the mic behaves normally until the user starts one on purpose.

**Billing error surfacing and paywall copy placement** (2026-08-24). `BillingManager.purchaseError: StateFlow<String?>` is set on any `BillingResponseCode` outside `OK`/`USER_CANCELED`/`ITEM_ALREADY_OWNED` - across the `purchasesUpdatedListener` else-branch and every early-return failure inside `launchPurchaseFlowFor` (failed product query, missing product details, failed `launchBillingFlow`) - each mapped to a short user-facing string by `errorMessageFor`. Before this, those paths only reset `_isPurchasing` with no message, so a failed purchase (network blip, store unavailable) looked identical to nothing happening. `SettingsScreen` shows it via a one-shot `Toast` (`LaunchedEffect(purchaseError)` + `vm.clearPurchaseError()`), the app's existing pattern for this kind of transient feedback - not a new dialog/banner. `PurchasableItemDef.unlockAlternative` (the free-earn path, e.g. "or play the metronome for 6 hours" for Glissie) renders **only** inside the purchase dialog (`PurchasableItemDialog`'s `belowDescription` slot), never in the Settings "Items" row above it - a deliberate, easy-to-forget split: the row is the pitch, the dialog carries the fine print.

**Drone usage tracking and the Drone Bee** (2026-09-03). Drone sounding time is its own counter (`MetroItemTracker.addDroneSeconds`, prefs key `drone_seconds`), credited in ten-second slices by `TunerViewModel.startDroneTimer` while the tone plays. Deliberately separate from `tuner_seconds` even though both happen on the Tuner screen: the two are mutually exclusive by design (the drone closes the mic), they earn at different rates, and an item asking for drone time must not be satisfiable by leaving the needle running. A paywall audition never counts, or the item would be farmable by opening a dialog.

**It earns 1 Gnote/minute capped at 30/day, the lowest rate in the app**, where everything else caps at 90. The drone is the only activity that asks nothing of the user once started, and it can sound at the same time as the metronome, which is already earning; matching the metronome's rate would have made "start both and walk away" the best-paying thing in the app.

**`PointsBannerQueue.postSilentEarn()` / `silentEarn` exists because of the cached-counter trap** documented under Gnotes/Points System. Continuous earners credit the tracker with no banner (a banner every ten seconds is intolerable), so the displayed total goes stale. The metronome got away with refreshing its own `_gnoteCount` inside its play loop only because the counter and the loop are in the same ViewModel; the drone's loop is in `TunerViewModel` and cannot reach it. Any credit path anywhere can now post this, and `MetronomeViewModel` (which owns the displayed total) recomputes. Use it for any future continuous earner rather than inventing a second private arrangement.

The reward is `items/DroneBee.kt` at `UnlockCondition.DroneSeconds(1_800)` (30 minutes): a bumblebee patrolling a figure of eight, because a drone is both a held note and a bee. Two art notes worth keeping: the path is a closed Lissajous rather than the random drift the Fireflies use, since a bee's motion has to look purposeful; and the body's heading comes from the derivative of that path, without which the sprite slides sideways through the corners and reads as a decal being dragged around. **One unit is `size.height / 18`, so a body under about 0.9u stops reading as a bee and becomes a striped dash** - it was first drawn at 0.62u and had to be scaled up.

**A moving item's motion must be expressed in `u`, and `previewRadius` must contain the whole path.** `ItemPreviewCanvas` (in `UnlockCelebrationOverlay.kt`, used by the collection cards, the unlock popup and the Settings dev previews) crops around `previewCenter` with a `u`-relative `previewRadius`, then zooms by `minDimension * 0.40 / radius`. If the item's drift is a fraction of the canvas instead, the two scale apart with aspect ratio: the Drone Bee's patrol was `0.085 * width`, which on a 200x150 collection card put its excursion at 96px inside a 100px half-width, so it spent half its loop outside the frame while looking correct in the taller Settings preview. Fireflies get away with canvas fractions because they genuinely are spread across the whole scene and their `previewRadius` is `6u`. For a single moving object, tie the span to `u` and size `previewRadius` to span + half the item, which makes containment arithmetic rather than luck.

**Analytics.** `drone_started` (note label, timbre, blend, reference_hz), `drone_stopped` (duration_seconds), `drone_voice_changed`, and `drone_voice_previewed` (product_id), all fired from `TunerViewModel` and defined in `AnalyticsTracker` like every other event. The start/stop pair mirrors the metronome's and the tuner's exactly, including the caveat they share: the stop event only fires on an explicit stop, so a session ended by the process dying is not counted. The note is sent as its label ("A3") rather than a MIDI number so a report reads without a lookup table, and it is the parameter worth watching (a pile of E, A and D says guitarists). `drone_voice_previewed` paired with the Play Console's sales for the same product id answers the only question a paywall really raises: of the people who heard it, how many bought. The Drone Bee's unlock needs nothing new, since `checkForNewUnlocks` already logs `item_unlocked`. Per-10s Gnote credits are deliberately not logged, matching the metronome: six events a minute of noise for a number already derivable from the durations.

**Adding an `UnlockCondition` variant: two of the four files will not remind you.** A new variant needs a counter in `MetroItemTracker` (writer, reader, `isUnlocked` branch, `resetAllProgress` line), a `displayText()` branch, and then `points/ConditionPoints.kt` (two functions) and `ui/components/metro_items/UnlockProgress.kt` (two functions). **`UnlockProgress.kt` is the one that gets missed** - it was, when `DroneSeconds` was added, and only the exhaustive `when` caught it. Nothing on the feature side references that file, so it never turns up in a search for the thing being built, and it drives the collection's progress bars, a screen away from wherever the work is happening. Both files are exhaustive `when` blocks on the sealed class, so the compiler is the safety net: expect those two build errors when adding a condition, and welcome them. The same checklist is repeated in `UnlockCondition.kt`'s own KDoc, which is the file you open first.

**Monetised drone voices** (2026-09-03). Reed and String (timbres) and Fifth (blend) are paid; Pure, Warm, Root and Octave stay free, so the drone is fully usable without paying. `billing/PremiumDroneDef.kt` maps the enum entries to products and is the only place that knows which voices cost money, keeping `audio/drone/` free of billing knowledge. Four things are worth knowing:

- **They share `BillingManager.SOUND_PRODUCTS`.** That set is "things you buy that make a sound", not "metronome clicks", so drone voices are priced, queried, reconciled, acknowledged and restored by the code that already does it for the clicks rather than a fourth near-identical family of flows. Nothing enumerates that set to build UI (the registries do that), so extending it is safe.
- **There is still exactly one `BillingManager`,** owned by `MetronomeViewModel`. `TunerViewModel` does not make its own: two clients would mean two listeners, two caches and two chances to disagree about what has been paid for. `MainActivity` gathers the state into a `billing/PurchaseStore` snapshot for `TunerScreen`, and pushes owned ids into `TunerViewModel.setOwnedProducts`, which reverts a locked timbre or blend to the free fallback (crossfading, so a refund mid-note does not cut). **That guard must not skip when the set is unchanged**: the field starts empty, so for a user who owns nothing the first push compares equal, and an early return would leave a paid voice selected from a previous install free to sound.
- **The failed-purchase Toast moved to `MetroGnomeApp`.** It lived in `SettingsScreen`, which was correct while Settings was the only place you could buy anything. With a second selling surface, a failure started from the Tuner would have gone unseen and then appeared as a stale toast the next time Settings opened.
- **An audition replaces the live voice, it never layers over it.** `DroneRenderer.setVoice` moves the old voice to `outgoing` and crossfades over 200 ms, after which `crossfade` reaches 1, `outgoing` is dropped and its level is multiplied by exactly zero. Measured residue is about -175 dB (the floating-point floor), and `DroneRendererTest.anAuditionReplacesTheLiveVoiceRatherThanLayeringOverIt` guards it by sounding Reed, auditioning Pure, and checking that Reed's third harmonic is gone. The dev asked this directly because it is genuinely unanswerable by ear: the two voices share a fundamental and the changeover is a fade. **The UI answers it too** - `PreviewActionButton` takes an `active` flag, and while an audition sounds the button turns gold, stops taking taps, and reads "Sounding <voice> on its own". Without that signal a listener has no way to tell a replacement from a blend, which is exactly the doubt that came up. Two related details: an audition started while the drone is *silent* floors the level at `MIN_PREVIEW_VOLUME` (0.35), because the saved level can legitimately be zero and a silent audition reads as a broken button, while an audition over a *sounding* drone leaves the level untouched so the comparison stays like for like; and `DroneKeyboard` carries a `contentDescription`, since it is a bare Canvas whose entire meaning is visual and a screen reader would otherwise find nothing where the note picker is.
- **The preview drives the real engine, not a sample.** `MetronomeViewModel.previewSound` fires a one-shot buffer because a click plays over anything; a drone is sustained and owns the speaker, so `TunerViewModel.previewDroneVoice` starts the actual drone at the note already chosen and the mic stands aside exactly as for a real session (a fourth holder in `resumeListening`'s list). If the drone is already sounding, the previewed voice crossfades in over the live one and back out, so the two can be compared directly. Its restore runs in a `finally` under `NonCancellable`, so a dismissed dialog or a real start mid-audition can never leave the previewed voice sounding.

Shared with the click sounds so the two paywalls cannot drift: `ui/components/PremiumChip.kt` (the gold star, which marks *premium* and stays after purchase, not *locked*) and `ui/dialogs/PremiumPurchaseDialog.kt`'s `AudioShowcase` (extracted from `SettingsScreen`'s private `SoundShowcase`). The dialog itself is the same `PremiumPurchaseDialog` down to the audible-preview slot.

**Premium sounds and instrument affinity.** `billing/PremiumSoundDef.kt` holds `PREMIUM_SOUND_REGISTRY`, the single source for purchasable sounds (product id, display name, description, `soundType` index); the current products are `sound_bell`, `sound_bowl`, `sound_kalimba`, `sound_cowbell` and `sound_metal_kick` (v6.5), all one-time in-app products the dev creates in Play Console by hand as step 5 of the recipe. Settings renders a chip plus a paywall dialog per entry automatically; billing reconciliation is generic over `BillingManager.SOUND_PRODUCTS`. To add one, follow the 5-step recipe documented at the top of `PremiumSoundDef.kt`. `ui/components/instruments/` is a text-free instrument-affinity nudge with one manager, `InstrumentAffinity` (maps a `soundType` index to the instruments it suits). Icons are Lucide glyphs (ISC, see `THIRD_PARTY_LICENSES.md`) rendered from path data via `PathParser` in `InstrumentGlyphs`. `InstrumentAffinityRow` sits above the Settings sound chips (matching instruments glow gold, the rest dim as the user taps through sounds); `InstrumentAffinityBadges` shows just the relevant instruments inside the premium paywall dialog so an unowned sound still advertises its fit.

**In-app polls and the shared feedback card** (reworked 2026-09-14, v5.23). `poll/PollConfig.kt` lists the questions (`ALL_POLLS`, each gated on `minGnotes`); `poll/PollManager.kt` decides what shows; `cloud/PollReporter.kt` writes one Firestore row per outcome. Three things the first version got wrong, all visible in its data (four rows in three months, three of them a timeout):

- **It lives on the Gnome tab only**, the app's front door, not the Rhythm tab where it sat from June to September and reached only the minority who open the game. State (`activePoll`, `pollDone`, `pollShowLogged`) is held in `MetronomeViewModel`, not the screen's `remember`, so a tab switch mid-show does not lose it. It is visible only at a genuine pause: metronome stopped, no Practice or Speed Trainer running, no practice result, What's New, unlock popup or mic check up. Starting the metronome hides it (nothing recorded); stopping brings it back.
- **There is no timer and no retry loop.** The old 25 s `auto_dismissed` re-asked daily and logged a row each time, which measured only that the user was busy on the screen. Now a *show* is counted (`recordShown`, one per day via a 24 h cooldown, `poll_shown` analytics event with `show_number`); after `MAX_SHOWS` (5) unanswered shows the poll retires itself and `retireIgnored()` hands its id back exactly once so a single `"ignored"` row is written. Outcomes are `up`, `down`, `dismissed`, `ignored`; `poll_answered` mirrors them to analytics.
- **`ui/components/FeedbackCard.kt` is the one asset for asking the user a question**: the bottom-sheet container (slide, surface, accent bar, shimmer) plus `FeedbackQuestion` (eyebrow, question at 16 sp, subtext, X, the opt-in dialog's ghost "no" / gold "yes" pair) and `FeedbackThanks`. `PollBanner` and `TunerFeedbackCard` are both thin users of it; the tuner card keeps its own timeouts (a reading goes stale, a poll does not) and its reason-chip step. It is deliberately bigger and bolder than the app's usual restraint: the dev's chosen balance between getting any feedback and nagging. Do not draw a new strip for a new question; use this.

**Groove Check route pre-flight and the L1 result** (2026-09-14). Field data showed "no output timestamp" (`NoteCode.NO_OUTPUT_TIMESTAMP`, L1) as the biggest failure cause: every Bluetooth run that reached the latency phase failed on it, and one budget phone logged four identical L1 failures in three minutes because the generic FAIL copy offered Try again. `MicCheckOverlay` now polls `AudioRouteMonitor.currentRoute()` on the intro and disables Start off the built-in speaker with a route-specific line ("Disconnect Bluetooth audio first", "Unplug headphones first"); a FAIL carrying L1 on a non-speaker route gets the fixable "switch to the speaker and try again" screen, and L1 on the speaker gets a terminal "Not available on this phone" screen with no retry. No gate logic changed, so `GATE_LOGIC_VERSION` was not bumped. The dev-tools "Mic self-test" button opens `MicDiagnosticsOverlay` (the engineering report), a different overlay, untouched.

**`theory/Meter.kt`** — Single source of truth for time-signature music theory (pure Kotlin, no Android deps, shared by the engine/ViewModel/Settings UI). Classifies a `Meter(top, bottom)` as SIMPLE (top 2-4), COMPOUND (top a multiple of 3, ≥6), or IRREGULAR (everything else: 5, 7, 8, 10, 11...) — classification depends only on the top number, never the bottom (3/8 is simple, not compound, despite the 8). `label()` renders this for display: "Simple quadruple", "Compound duple", and for IRREGULAR either **"Odd"** (when the top number is actually odd — 5, 7, 11, 13) or **"Irregular"** (when it isn't — 8, 10, 14, 16). Do not collapse this back to a single "Odd" label for the whole IRREGULAR class: "odd meter" specifically means an odd number of beats, and mislabeling an even one is a factual music-theory error a musician would catch (caught and fixed 2026-08-14, see `MeterTheoryTest`'s parity test cases). `description()` gives the plain-English feel ("Felt in 2+2+3" for 7/8, "Felt in 2, beats split in three" for 6/8), rendered by `ui/components/TimeSignaturePicker.kt` above the accent-cell grid it explains. `beatGrouping()`/`defaultAccents()` derive the natural accent pattern from the same classification (irregular meters default to twos with a trailing three, e.g. 7 → [2,2,3]) — the Settings UI lets the user override any accent, since the "one true" grouping for an odd meter is genuinely contested.

**Chord Finder** (v6.0, the fifth tab, `AppTab.CHORDS`, between Tuner and Rhythm; started 2026-09-15). It is a tab rather than a page inside the Tuner for the drone's reason: a feature behind something else is found only by people who go exploring, and this one is meant to carry the 6.0 marketing. Three layers, each testable on its own:

- `theory/Chord.kt` is the sibling of `Meter.kt`: pure Kotlin, `ChordTheory.identify(midiNotes)` returns a `ChordReading` (`Empty`/`Single`/`Dyad`/`Identified`/`Unnamed`). Chord types are defined by **degrees** ("1 ♭3 5 ♭7"), not semitone offsets, because degrees are what spell every chord tone correctly from the root (the seventh of B♭7 is A♭, not G#; `ChordMatch.spell`). **The bass note decides** between readings with the same pitch classes (C E G A is C6 with C lowest, Am7 with A lowest; the loser is offered as an alternative, as a slash chord if its root is not the bass), which is why callers pass real MIDI notes and never bare pitch classes. A seventh chord may be matched without its fifth ("C7(no 5)", the everyday guitar voicing) only when it has a third and a seventh; triads, sus and added-tone chords never lose their fifth, because dropping it leaves a set some other reading explains better (C D E is not "D7sus2(no 5)/C", which the first version said). `ChordTheoryTest` covers naming, ranking, spelling and dictionary integrity (no two types share a pitch-class set; every type round-trips through `identify`).
  - **Spelling rules.** A chord tone's letter is the root's letter advanced by the degree (a third is two letters up, a seventh six) and its accidental is whatever closes the gap to the actual pitch, so C#7 contains **E#** and D♭7 contains **C♭**; these are correct and must not be "fixed" to F and B, which are different note names for the same pitch in a different chord. Root spelling follows common practice, not one rule (`ChordTheory.rootName`): flats for major-type roots on pitch classes 1, 3, 8, 10 (D♭ E♭ A♭ B♭) and sharps for minor-type roots on 1 and 8 (C#m, G#m); F# always. Readings with no chord root (a single note, a plain interval, an unnamed set) use `NoteNames`' sharp names, matching the tuner. Never build a second name table for chord tones anywhere in the UI; everything goes through `ChordMatch.spell`.
  - **Octave numbers follow the spelled letter**, not the MIDI number (scientific pitch notation): the seventh of D♭7 sounding at the pitch B3 is written **C♭4**, and the seventh of C#m(maj7) at C4 is **B#3**. `ChordMatch.spelledOctave(midi)` is the only correct source for the octave beside a spelled name; `NoteNames.octaveOf(midi)` is right only for a sharp/natural name. This was a real bug in the first note chips (they printed C♭3), invisible until a chord that spells C♭ or B# came up.
  - **The "about" lines are checked facts, not colour.** Every claim in them was verified (audit 2026-09-15): the James Bond chord is the minor-major *ninth* (Em(maj9)), not m(maj7); C6 shares its notes with the m7 a *minor* third below; an eleventh chord is normally voiced without the third, which is why it equals a 9sus4. The dev cannot verify music theory himself, so anything displayed here must be checked against a reference before it is written, the way the tempo-name table once had to be corrected.
- `viewmodel/ChordFinderViewModel.kt` owns its **own `Tuner()` instance** rather than borrowing `TunerViewModel`'s, because that one is tied to the Tuner tab's usage timer, analytics session and feedback prompt. Notes arrive from a tap on the drawn instrument or from the mic one at a time (arpeggio capture: a note is added after `CAPTURE_FRAMES` consecutive tuner readings on it, and the same pitch is not re-added until the reading has gone null, so a sustained note cannot re-add itself after being removed). The tuner's reference pitch and calibration factor are read from `tuner_prefs` so both features agree on what "E3" is. **A new arpeggio starts from the bottom**: a mic-captured note below the current bass, once the set holds three notes (`RESTART_MIN_NOTES`), starts a new set with that note. From the notes alone "next chord" and "extension" are the same stream (C E G then G B D is a perfectly good Cmaj9), so the boundary has to come from outside them, and this is the one signal that costs the player nothing because the page already teaches that the lowest note is the bass. Extensions live on top and still extend; the one miss is a lower bass added for a slash chord, which a tap handles (tapped notes are exempt). A silence timer was considered and rejected by the dev: pausing to think about the seventh must not cost the triad. Manual Clear stays. The tip strip teaches the rule once a chord is named. Strummed chords are deliberately out of scope: `PitchDetector` is monophonic MPM and a chord has no single period to find; real polyphonic recognition would be a new engine (chroma + templates), not a tuner tweak.
- `ui/screens/ChordFinderScreen.kt` is the tab's screen, a `when (currentTab)` branch like the others, with **no heading** (no tab has one; the nav bar names it). `switchTab` stops a sounding drone when landing on Chords, for the tuner's reason: the mic would hear the app's own tone; and Chords counts as a calm, ad-free tab for the review prompt alongside Tuner and Settings. The tab icon is `ui/components/ChordGridIcon.kt`, a drawn chord-diagram glyph sized like `TunerNeedleIcon`: three strings, three fret spaces, a heavy nut and two dots stepping down the grid (the first cut had four of each and three dots, and beside the needle and the pulse it was the busiest thing on the bar; a dot in the first space crowds the nut). **Order is chord card, mic strip, instrument, notes strip, tip strip**, 12 dp apart, with the banner ad pinned under the scrolling column exactly as on the other tabs (`isAdFree` passed from `MetroGnomeApp`; no interstitials, it is a calm tab). The chord card leads because in its empty state its eyebrow reads "CHORD FINDER", the only place the feature is named (tabs carry no heading, but a beginner needs the words somewhere); once notes are in, the eyebrow becomes the reading's label. **No-jump rule, not taste**: every element a reading changes has a fixed height whatever it holds: the chord card pins one line of name and two of description (every `about` string is kept under 86 characters so it cannot reach a third line at 11 sp on a 280 dp column), the notes strip is one scrolling row with a "No notes yet" placeholder and an always-present dimmed Clear, and the tip strip is a fixed 30 dp row whose text may wrap to two lines. Nothing appears or disappears; only words and colours change. **"Pinned" must mean a density-sized `Box`, not a text setting** (`PinnedSlot` in the screen, 2026-09-16): `minLines`/`maxLines`, and even `LineHeightStyle.Trim.None`, still left the card shifting 1 to 2 dp between readings, because with font padding off Compose adds a top/bottom padding to every `Text` from the ink bounds of the glyphs on its first and last line (`TextLayout.getVerticalPaddings`), so a 44 sp "C" and a 24 sp "Play or tap notes" on the same 50 sp line measure differently. The dev noticed the jump by eye; it was confirmed by measuring his screenshots pixel by pixel, which is the way to settle a "subtle shift" report rather than reasoning from the text APIs. See the Typography trap section. The tip strip carries a bare gold icon (bulb, or swap arrows when alternatives show) and no "NEXT"/"ALSO" word, with the mic strip's 12 dp after the icon; the first cut had a disc behind the icon and a caption, and both went for consistency with the mic strip. Guitar is the default instrument (the app was built for a guitarist) and instrument, notes and the mic on/off choice all persist in `chord_finder_prefs`, so the page reopens as it was left.
- **Hearing a chord back** (2026-09-15, after the web tool's "hear it" was liked). `audio/chords/` is split the drone's way, one job per file: `ChordVoice.kt` (pure Kotlin synthesis: a list of `ToneEvent`s mixed into one 16-bit buffer, plucked envelope, harmonic partials, normalised only if it would clip), `ChordPlaybackPlan.kt` (`ChordPlaybackPace` SLOW/QUICK and the "arpeggio from the bass, breath, then together" event list; QUICK is the web tool's timings exactly), `ChordPlayer.kt` (static `AudioTrack`, blocking play with a 50 ms stop check, `playing` flow). **The dev tools' test tone is the same synthesis**: `ChordArpeggioTestTone` builds events from its `ChordTestTimings` and renders with `ChordTimbre.TEST_TONE` (bright to the fifth partial, no decay, for the tuner to lock on) through its own `ChordPlayer`; "hear it" uses `ChordTimbre.PLAYBACK`. Do not add a third tone generator for chords; add a `ChordTimbre` or a plan. `ChordFinderViewModel.hearChord` does not close the mic (a two-second phrase is not worth the drone's permission round trip); instead `captureFromMic` ignores readings while `captureMuted` is set, which lasts the phrase plus `PLAYBACK_MUTE_TAIL_MS` (600 ms), because the closing block chord is exactly the steady sound the tuner would lock on and feed back as a wrong octave. The pace persists in `chord_finder_prefs` (`playback_pace`). UI is `HearStrip` on the Chords tab between the notes strip and the tip strip, laid out since 2026-09-21 as the Listening card's twin (header whose value is "Sounding" in gold while it plays and nothing otherwise, the pace being the selected chip; Slow/Quick chips sharing a row with **`ui/components/PlayStopKey.kt`** and centred on it; a pinned two-line caption), after a stacked label-over-chips layout read as squashed beside the Listening card; the drone's purple-play/red-stop `RaisedControl` disc extracted into a shared composable (DronePanel now uses it too), so the app's two "start a sound" keys are one asset; a tap while sounding stops. The first cut put label, chips and key on one line, and on the dev's phone the subtitle was left with a few dozen dp and ellipsed ("Arpeggio, then tog..."); stacking the chips under the label gives the text the whole width left of the key, and the key spanning both lines is what makes it read as the row's control rather than a third chip. With no notes the key dims and the chips grey out and go inert, because lit pills beside a dim key read as the thing to tap to start the sound. It was placed on its own row deliberately: a button beside Clear cramped the notes strip, and the page had room on tall screens. `chord_played` (symbol or "none", note_count, pace) is logged on the tap. `ChordVoiceTest` checks length, clipping, and that `PitchDetector` reads every arpeggio note within 5 cents.
- **`ui/components/MicAccessStrip.kt` is the one way a screen asks for microphone access**: a slim tappable strip (crossed-out mic in the warning colour, "Microphone access needed, tap to grant" or "Microphone blocked, tap to open App Settings", chevron), the same shape and height as the Chords mic strip, which shows it in place of its level meter until the grant arrives. It replaced the tuner's card-with-a-button, which took three times the height to say the same thing. `MicCheckOverlay` keeps its dialog copy (mid-flow, with retry and cancel) and `MicTimingNudge` its gold pill (one state among several in that slot); any new screen that needs the mic uses this strip.
- **The Listening card** (2026-09-21, replacing a mic strip above the instrument and a separate engine strip) sits **under the instrument, above the notes strip**, in the tuner's card grammar, which is the page's consistency reference: `surfaceDim`, 16 dp radius, 18/16 dp padding, a 12 sp bold caps header with 1 sp tracking and a right-aligned 14 sp bold value that says only what the chips below do not (the heard note in gold, or "Paused"; a first cut repeated the selected mode there and the dev called it redundant), 14 dp gaps, 11/14 sp `textMuted` captions. Rows: the mic row (toggle, level meter, badge; or the microphone ask as a row in the same slot until the grant arrives), two equal-width Fast/Steady chips, and a pinned two-line caption that crossfades with the choice. Every slot is density-sized, so nothing below moves. The dev asked for it after the 10 sp strips read as "a spit and paste job" against the tuner page; **any new card on this page uses this grammar**, and the other Chords cards (10 sp headers) are the ones out of step now. The mic row is built from **`ui/components/ListeningIndicators.kt`**: `InputLevelMeter`, `ListeningStateIcons` (the six-icon row) and `ListeningStateBadge` (the same indicator as one crossfading badge; the Chords page passes `quiet = true`, which shows only the ear and the lock, because the dev found the four in-between icons distracting while playing; the tuner keeps all six), plus `listeningStateColor`, all extracted from `TunerScreen`'s private copies so the two screens cannot drift. The note chips all look alike: a first cut rimmed the first-arrived chip in gold as "the bass", which it was not, and the gold "R" already marks the root. It reuses `ui/components/PianoKeyboard.kt` (extracted from `DronePanel`'s private keyboard, now multi-octave with `litMidi`, optional octave labels and a capture `glow`) and the new `ui/components/GuitarFretboard.kt` (standard tuning, 12 frets, every position of a lit note marked, degree labels inside the markers, fret numbers in their own band under the neck). The mic permission flow mirrors the tuner's directly rather than `MicPermissionRecovery`, which is Groove Check's and arms `micModeEnabled` on grant, with one addition the tuner lacks: the grant is re-read on `ON_RESUME`, because the only way past "permanently denied" is a round trip through App Settings and the strip would otherwise keep saying "tap to grant" after the user had. The ViewModel's `init` block sits **below every property**, deliberately: `viewModelScope.launch` on the main thread runs a coroutine to its first suspension at once and `StateFlow.collect` delivers the current value before suspending, so the two collectors touch capture and session state synchronously from inside `init`.
- **Instrument card details.** The card's caps label states what is loaded, in the drone header's idiom, not what the card is: "STANDARD TUNING" on the guitar (the one fact a drop-D player needs before tapping anything; the string names were tried and read as clutter) and "4 OCTAVES · C2 TO B5" on the piano, derived from the keyboard's constants. Both instruments draw at one height so switching never moves what is below. The 4-octave keyboard is `PianoKeyboard` with C2..B5, opening scrolled to C3; the guitar is 12 frets. **A note captured from the mic eases the instrument to centre it** (450 ms `FastOutSlowInEasing`, keyed on a per-capture serial so a repeated note still scrolls; on the guitar the nearest of the positions that sound it, so the neck moves as little as possible), because a low E lands far left and a high note far right, where the capture glow went unseen. Geometry for that comes from `pianoKeyCentreFraction` and `fretboardPositionFractions`, exported by the two components so the screen cannot disagree with where a key is painted. Tapped notes never scroll. The dev did not realise the keyboard scrolled at all, so the shared `FadingHorizontalScrollbar` gained an optional `hint` ("‹ scroll ›" on a pill of the card colour, fading with the bar, 14 dp tall via `SCROLLBAR_HINT_HEIGHT`); the two chip-row call sites are untouched.
- **Vocabulary.** The feature is the "Chord Finder" (the search term, and what the page does); "arpeggio" is the *instruction* for the mic path, used in the empty-state copy ("Play the chord as an arpeggio") and in marketing body copy, never as the feature's name, since people searching "arpeggio" want exercises. The nav label is "Chords" (five tabs leave no room for more).
- **The Acoustic Guitar item** (`items/AcousticGuitar.kt`, `UnlockCondition.ChordsDiscovered(25)`) is the tab's reward. The counter is a **set** of distinct chord symbols (root + quality, so "C" and "C/E" are one chord), recorded by `ChordFinderViewModel` in the same 1.5 s hold that logs `chord_named`, so it cannot be farmed by re-naming one chord; twenty-five is chosen because the twelve major and twelve minor triads make twenty-four. The item itself is not Gnotes-framed (`ConditionPoints` returns 0 and names the action, like `MicChecksCompleted`), because the item counts *distinct* chords while Gnotes count *every* chord.
- **Gnotes: 5 per chord named, 9 chords a day (45/day)**, `PointsConfig.PER_CHORD_NAMED` and `PointsLimits.CHORDS_NAMED_PER_DAY`, credited from the same 1.5 s hold as the analytics and the item, with `PointsBannerQueue.postSilentEarn()` so the Gnome pill refreshes (a banner every few seconds would be intolerable). Half the practice activities and above the drone: it is a lookup tool, and a chord can be tapped in as easily as played. The counter is `MetroItemTracker.recordChordNamed(fromMic)`, which also keeps a **mic/tap split** (`chords_named_mic`, `chords_named_tap`) from day one so a future item can reward playing chords in specifically; nothing reads the split yet. The "Chord Finder" row appears in `EARN_RULES` (icon key "chords"), `PointsCalculator` (both `calculate` and `todayBeats`), `DailyActivity`/`DailyActivityLog` (`chordsNamedToday` with its baseline) and `PointsManager`; `RewardConfig.MAX_DAILY_BEATS` is derived from the rules, so the ad-free reward threshold rose by 45 automatically. Pacing math: at the daily cap a user who names only new chords reaches the guitar's 25 distinct in 3 days; in practice a session yields four or five new distinct chords once the obvious ones are used up, so five days or more is typical. A hard "at least N days" floor would need a distinct-days condition, which no item uses. The ViewModel carries its own `unlockQueue`/`checkForNewUnlocks`/`markCelebrated` mirroring `RhythmGameViewModel`, and the screen shows `UnlockCelebrationOverlay`, so the popup appears on the tab where it was earned. Body outline follows real classical measurements (lower bout 0.72 of body length wide, waist 0.48, upper bout 0.57, flat-ish bottom and top); the first cut was two near-circles with a deep pinch and the dev read it as a figure of eight, so keep the bout/waist control points vertical. Scene position: propped against the forest tree, drawn at 0.6 scale (3.3u tall, the music stand's size; at full scale it stood beside Metro on his plane instead of back at the tree), foot 0.55u right of the trunk line (`size.width * 0.10`, width-anchored like the tree) on the ground, leaning 10 degrees back onto the trunk, drawn upright and rotated about its contact point so the strings stay parallel to the neck; it first stood at cx + 3.9u between Metro and the torch post, and the dev asked for it under the tree instead. Its body covers the trunk's moss patch and most of the carved note; move the carving up the trunk if that detail is wanted back. `usage/ActivitySummary` (the dev-only profile snapshot) carries `chordsDiscovered` as a set (merge = union), and `droneSeconds` and `micChecksCompleted` were added alongside it, having been missed when those features shipped: **a new progress counter is not done until it is in the summary, the logger, the restorer's apply and merge, and the round-trip overlay's three row lists.**
- **Dev tools: the Chord Loop.** `debug/chords/` holds a closed loop for the mic path on the developer's own hardware, since it cannot be tried on an emulator and cannot keep costing Console uploads. There is **one** entry point: the Settings dev button "Chord Test UI", which opens `ChordLoopDiagnosticOverlay` and never starts a run itself; while a run is in progress the same button becomes "Cancel Chord Test" (red) and a tap cancels. Dev button labels are kept to three or four words: a first cut with a parenthetical explainer was too long to fit every phone. A plain "Play Test Chords" button that played the sequence without recording anything was removed 2026-09-15 along with its plumbing (`playWhenListening`, its scope and `playing` flow); the dev found a button that plays but reports nothing pointless, and could not tell before tapping which of the two started a run. The overlay's Run plays C4 major, G3 seventh and E3 minor as arpeggios through the speaker **the moment the Chords tab's mic opens** (it waits on `ChordFinderViewModel.listening`, up to 60 s; a 3 s timer was tried first and was a race), and `ChordLoopRunningPill`, overlaid at the bottom of the Chords tab, shows the run is live (pulsing dot, round, which chord of three, "tap to cancel"), because without it the only sign a run was going was waiting to hear the next note. It composes to nothing when no run is in progress, which a release build always is, so it needs no debug gate. `ChordLoopDiagnostic` plays one chord, watches the ViewModel's own flows (`captured`, `heard`, `ambient`, `notes`, `reading`), compares with what was played, classifies each missed note by its signature in the tuner's state (not heard / heard but not captured / held previous / octave error / wrong pitch / extras), turns the one knob that signature points at (level, note length, gap, octave shift), and goes again, up to 8 rounds, until every chord's notes and name come back exact; then the timings are saved in `chord_test_prefs` and later runs start from them. **The default timings are derived from the ambient gate, then corrected by measurement**: a lock rides out up to 800 ms of "disturbance" (a new note counts) and 360 ms of silence, both up to doubled by the suppression scale, then 320 ms to acquire plus three readings to capture; the derivation gave 1500/900, but the first real phone still showed "held previous" on the second note of the two chords below middle C at that gap and passed in round two at 1700/1300, so those are the defaults now (the first cut, 900/250, failed the same way). There is no "reset tone": the loop clears the finder programmatically between chords. Four properties worth keeping: **every adjustment moves one way** (louder, longer, wider, up an octave) under hard clamps, so the loop converges or hits a ceiling and cannot oscillate; **the chord name is checked, not just the notes** (`ChordResult.pass` requires `gotSymbol == expectedSymbol`, and a wrong name with all notes present is reported as a theory issue rather than a knob to turn); **readings and states already in force when a note's window opens count** (they are logged on change, so a lock still sitting on the previous note never changes inside the window and would otherwise read as "not heard"); and **the watchers live in the object's own scope and are released in a `finally`**, so a cancel mid-chord does not leave three collectors on the ViewModel's flows. `ChordFinderViewModel.diagnosticMode` is true for the length of a run, and the naming hold job returns before analytics, Gnotes, the discovered-chord set and unlock checks, so a run from a developer phone earns and logs nothing. "Reset timings" in the overlay clears `chord_test_prefs` (disabled while a run is in progress). The vibration felt during a run is the finder's own capture haptic (`HapticPattern.TICK` through `HapticEngine`); the Tuner screen gives the same tick on the QUIET to LOCKED edge (`distinctUntilChanged` on `ambient.locked`, so a held lock never repeats it), so the two listening screens feel like one instrument.
- **Two listening engines** (2026-09-21, shipped in v6.5/87 on `v6.0`), offered to the player as **Fast / Steady** on the Chords page's **Listening card** (`chord_finder_prefs` key `engine`, `ChordEngine.DEFAULT` = Fast/Brossier since the 2026-09-21 phone battery). The names in code stay McLeod/Brossier (`methodName`, used by the Chord Loop and logs); `displayName` and `caption` are the player's words: Fast "Hears each note as you play it, even quick arpeggios. Best in a quiet room.", Steady "Only counts a note you let ring a moment. Less sensitive to your surroundings." The captions say what the player gains and gives up and nothing about how; a first cut said "Ignores talking and bumps" and the dev vetoed it as too literal (people would think only those two). The choice was briefly dev-gated; the dev decided a musician should have it, since Fast may be too sensitive for some rooms. `ChordFinderViewModel` owns both, exactly one running; `setEngine` reopens the mic on the other, the strip's `heard`/`amplitude`/`ambient` flows follow the active one (`combine`), and both feed one `admit(midi)` so the restart-from-the-bottom rule and the note-set rules are shared. The dev asked for this after a YouTube arpeggio with no gaps failed on the shipped path, and the audit confirmed it is the design, not the video:
  - **McLeod** is the tuner's pipeline unchanged (`Tuner` + `AmbientDetector`). Three of its mechanisms, each right for a tuner, stack against a note sequence: a new note over a ringing one is a *disturbance* ridden out for `HOLD_DISTURB_MS` (800 ms, up to 2x under `holdScale()`, which any pluck transient drives to the cap in a quiet room); the presence probe (`presenceAt` at the old note's lag, `HOLD_CLARITY` 0.65) resets that ride-out while the old note still rings, so an open-string arpeggio can hold note 1 indefinitely; and after the drop, `ACQUIRE_MS` (320) plus `CAPTURE_FRAMES` (3) at a 93 ms hop. Measured: ~0.9 s from pluck to the first note appearing, 1.7 to 2.5 s or never for a note over a ringing one. This is why the Chord Loop's spaced defaults had to be 1700/1300. Do not "fix" the tuner for this; its hold is what tuning needs.
  - **Brossier** is `audio/chords/NoteAnalyzer.kt` (pure chain) + `NoteCapture.kt` (its own `AudioRecord`, the tuner's split), the literature's monophonic note-labelling model (Brossier, Bello & Plumbley 2004, aubio `notes`): an onset marks a note, the attack is skipped, the pitch is the median of the frames after it, no hold. `audio/dsp/OnsetDetector.kt` is SuperFlux (BÃ¶ck & Widmer 2013: quarter-tone log filterbank, max filter over neighbouring bands of the previous frame so vibrato is not an onset, `log10(1 + 100m)` compression) with madmom's online peak picking plus a **relative** criterion (3x the local mean and at least 0.35), because with log compression the additive threshold alone scales the detector to the player's volume: it missed a -48 dBFS pluck, a re-pluck of a still-ringing string (rise 1.68 over a local mean of 0.45) and the third note of a quiet arpeggio, while steady room noise sits at 0.15. Window 4096 / hop 512 (the tuner's 8192/4096 is for cents; a chord needs the nearest semitone).
  - **MPM is monophonic and two notes at a consonant interval read as their common period.** C4 and G4 together, equal level, read as C3 (`PitchDetectorResidualTest` documents it), and the new note's own NSDF peak is under `PEAK_PICK_RATIO` unless it is ~10 dB above the old one, so the plain reading during an overlap is the missing fundamental, not the new note. The fix is `PitchDetector.detectAbove(window, reference)`: spectral subtraction (the presence probe's whitening step, generalised; `RESIDUAL_OVERSUBTRACT` 2.0) of the magnitude spectrum taken 4 hops before the onset, then MPM on the residual, which hears only what is new. `NoteTracker` prefers the residual for its evidence. This is what makes the descending and the never-decaying cases pass.
  - **`NoteTracker` has two guarded fallbacks** for notes without a clean attack: a rise from silence counts as an onset, and a steady new pitch (0.85 clarity, 25 cents, 140 ms) counts as a note but is refused when it is a sub-harmonic (integer division 2..8) of any note emitted in the last 4 s or a note still in that memory (an old note re-emerging as the newer one decays). The flux-onset path takes neither guard, since a real pluck of a low note has an onset. The live readout while a note sustains is the decided note, not the mix's virtual fundamental. `forget()` clears the memory on Clear and after "hear it". Every `Note` carries its `Source` (FLUX, LEVEL, STEADY), which the Chord Loop log prints.
  - **The rise-from-silence path decides at the steady path's 0.85 clarity, not the residual's 0.5** (2026-09-21, the first phone run of the Chord Loop's spaced mode on Brossier). Every real note came back at ~300 ms, and then a low phantom (A1..C3, clarity ~0.6) appeared in the gap after it; being below the bass, each one triggered the restart rule and wiped the chord, so the screen showed "C3" for C major. Reproduced on the JVM (`NoteAnalyzerTest.theChordLoopsSpacedTestToneOverAgcLiftedHumAddsNothing`): a phone mic's automatic gain lifts the room floor in every gap after a loud note, the hum crosses the tracker's `loud` bar (floor x 2), and that *is* a "rise from silence", with nothing armed for a residual, so the plain pitch of hum was weighed at the bar meant for the residual of a mix. A rise from silence cannot be a mix, so it is held to the steady path's bar; a real soft note reads above 0.9.
  - **Fast when sure, patient when not** (2026-09-21, same day, from the first full phone log): the tracker's 73 ms decision is right for a note and wrong for anything else that is briefly pitched. With dogs barking outside it named barks, a hadeda and a cleared throat: all at -45 to -35 dBFS, swelling over ~200 ms with clarity climbing 0.6 to 0.89 and the pitch gliding 85 to 180 cents, while a played note hits -15 dBFS at clarity 0.9+ inside 40 ms and holds within 2 cents. So `NoteTracker.decide` has two tiers: evidence whose median RMS is at least `STRONG_RMS` (0.04, about -28 dBFS) *and* mean clarity at least `STRONG_CLARITY` (0.9) is decided the old fast way; anything weaker must hold within 30 cents on 14 frames inside a 300 ms window (`PATIENT_*`), which a bark never does. `Note.patient` says which tier decided it, and the Chord Loop log prints it. The dev asked for this independently ("it should still be sustained") while the fix was being written.
  - **An onset that brings nothing new is not an onset** (`NoteAnalyzer.NEW_POWER_MIN`, 2026-09-21): at high volume the phone's limiter re-triggers the onset detector a few hundred ms into every sustained sound. On a single note that re-decided the same note (harmless); on a *ringing chord* it decided the chord's missing fundamental (D#2 under C E G, G1 under G B D), loud and at clarity 0.9, because the subtraction floor (`NOISE_SPECTRAL_FLOOR`, 10%) leaves a ghost of everything that was already sounding and MPM is level-blind. `PitchDetector.lastResidualPowerFraction` now says how much of the window's power the pre-onset spectrum did *not* explain; under 5% the analyzer does not pass the onset to the tracker. And a flux window is judged on the residual only, never falling back to the plain pitch of the mix (`NoteAnalyzerTest.aReTriggeredOnsetOnARingingChordAddsNothing` reproduces the phone: without the gate it names C2). Both fixes were driven from the log, verified on the JVM first, then on the phone: Spaced passes in round 1 at the defaults and Legato on Brossier is 10/10 with a ~330 ms median (speaker latency included), repeatably.
  - **The ambient detector was audited against that noisy log and is not broken** (2026-09-21): of 16 non-note sounds that reached the tuner's clarity bar, the longest held it for 142 ms, against a 320 ms / 18-cent acquire rule; all 30 notes held 2 s within 9 cents. Source identification (bark vs bird vs car) would add nothing to the gate; duration and stability already separate everything except a sustained steady tone such as an alarm, which no temporal rule can tell from a note and only profiling can (it was there before the player started). Two small shared improvements came out of it, JVM-tested, then run through the full phone battery before shipping: (a) `NoteTracker` now profiles for 900 ms like the tuner and learns a room tone the same way (`HUM_*` constants copied; a pitch on it while near the floor is not evidence), after a silent room named G1 (mains) at mic-open; (b) both `AmbientDetector` and `NoteTracker` ratchet their floor *upward* on loud tone-free frames by a fixed 0.4%/93 ms (`FLOOR_RISE`, a doubling every 16 s), because each could previously only follow sound below twice itself, so a room that got louder mid-session froze the floor for good. Geometric, not averaged, so a two-second slam moves it under 10% while a room twenty times louder is absorbed in about 70 s. Close, loud speech or laughter next to the phone still registers as notes on Brossier by design: it is as loud and briefly as clear as a plucked string, and the finder is meant to hear what is played in a reasonably quiet room, not to tell a voice from an instrument. **Measured** (Chord Loop **Listen mode**, added for this: plays nothing, records 30 s of the engine hearing the room, every decision an "extra" with its frames): a laughed "ha ha ha" at 349 Hz held within 6 to 9 cents at clarity 0.98 for the fast window, against 0.7 cents median / 3.3 cents p95 for 123 real notes; nothing the engine measures inside 70 ms separates them, only duration would (a "ha" is over in ~150 ms). A confirmation stage (emit only if the pitch is still there ~120 ms after the decision) would reject bursts at the cost of ~120 ms latency and was left to the dev's call. The steady path's hold was raised from 140 to 300 ms (`STEADY_MS`, the patient rule's length) after a spoken vowel at low level was named through it. Silence control on the final build: 30 s, zero onsets, zero notes.
  - **Measured on the JVM** (`NoteAnalyzerTest`, rendered by `ChordVoice`): decision 73 ms after the first pluck, 110 to 120 ms after a note plucked over a ringing one, on legato at 450 ms per note with 900 ms holds, a never-decaying chord, a descending line, a lower-bass second chord, low E2 and high E5, a quiet player in a noisy room, a bowed swell. `OnsetDetectorTest`, `NoteTrackerTest` (each rule on synthetic frames) and `PitchDetectorResidualTest` cover the parts.
  - **The Chord Loop keeps everything it observed and can save it as a file** (2026-09-21). Each `ChordResult` retains the raw timeline (`events`: every capture, reading change and state change, timestamped from the chord's nominal start) and, on Brossier, `frames`: the analyzer's per-hop trace (`NoteAnalyzer.Trace`, via `NoteCapture.trace` and `ChordFinderViewModel.setFrameTrace`, set only for the length of a chord and cleared in `observe()`'s `finally`): rms, flux, onset flag, plain and residual pitch with clarity, tracker state, `loud`, floor, and any note decided. The overlay's "Save Log" button serialises the whole run (`ChordLoopReportJson.kt`, `org.json`, frames as column arrays because key names would be most of the file) through the system `CreateDocument` picker, where Drive is a destination; no FileProvider, no permission. Every run also writes `files/chord_loop_last.json` when it ends, whatever the outcome, so with the phone on USB the whole loop can be driven and read from the PC (`adb shell run-as com.wynandl.metrognome cat files/chord_loop_last.json`; `uiautomator dump` finds the dev buttons by text, and `pm grant ... RECORD_AUDIO` after a reinstall). That is how the 2026-09-21 fixes were found and verified in one sitting, and it is the workflow for any future engine change: the screenshots the first run was diagnosed from are not it. **`ChordLoopRunningPill` carries a progress bar**, one step per note by the clock (`State.progressAt`, from `chordStartsAt` and the note slot) over the current pass: a spaced round, with "round n of 8 max" in the text, or the whole legato comparison. The dev cancelled the first spaced run because nothing said how long it might go on.
  - **The Chord Loop has a Legato mode** (chips Spaced / Legato in `ChordLoopDiagnosticOverlay`): plays the three chords with `LEGATO_STEP_MS` 450 and `LEGATO_HOLD_MS` 900 in `ChordTimbre.LEGATO_TEST` (bright partials with a pluck decay) once per engine, restoring the page's choice after (also on cancel), and shows one PASS/FAIL card per engine with notes and chords caught and the median capture delay (from the note's nominal start, speaker latency included, the same for both). Attribution is by pitch, not slot, since the notes overlap. The verdict sentence is computed from the numbers, never assumed. Spaced mode is unchanged and names the engine it ran on. `chords_session_started` now carries `engine`; `chords_engine_changed` logs a switch.
- **Analytics** (defined in `AnalyticsTracker` like every other event): `chords_session_started` (instrument, mic = on/off/denied, engine) and `chords_session_ended` (duration_seconds, tap_notes, mic_notes, chords_named) bracket one visit to the tab, fired from the screen's `DisposableEffect(Unit)` via `onScreenEntered`/`onScreenLeft`; `chord_named` (symbol, note_count, source = tap/mic) fires only after a symbol has **held for `NAME_HOLD_MS` (1.5 s)**, so the triad a four-note chord passes through on its way in is not counted; `chords_instrument_changed` on a real switch. Two guards matter: the ViewModel is created at app start with the persisted notes already loaded, so naming is only tracked while `sessionActive`, and a chord restored from the last visit is pre-marked as already named on entry so it is not logged again. The mic/tap split is the number to watch; the feature's design was a bet that people would play chords in.

**`viewmodel/MetronomeViewModel.kt`** — Manages play state, BPM, time signature, sound type, volume. Tap tempo requires ≥2 taps within 2.5s. Persists settings to SharedPreferences (`metrognome_prefs`).

**`viewmodel/RhythmGameViewModel.kt`** — State machine: IDLE → COUNTDOWN → PLAYING → RESULT. Notes have 2000ms travel time from spawn to hit line. Hit windows: PERFECT ±50ms, GOOD ±100ms, ALMOST ±150ms (scaled by user tolerance setting). Scoring: PERFECT=100, GOOD=70, ALMOST=30. High scores persisted per difficulty in `rhythm_highscores` SharedPreferences.

**`ui/components/GnomeCanvas.kt`** — Custom Compose Canvas drawing the gnome. Animations (pendulum, bounce, flash, twinkle) are synchronized to `BeatEvent` emissions from MetronomeViewModel. The `feature/metro-cosmetics` branch extends this with a `MetroItem` system: `activeItems`/`onItemTapped` params, tap-hit detection, and separate draw passes for background items, body-attached items, and head-attached items (which bob with the head group).

**Metro's lighting model** (v5.19) — every part of the character is lit by one key light from the **upper-left**, driven from a single place rather than per-feature approximations. `KEY_LIGHT_X/Z` + `rollLambert(nx)` in `GnomeCanvas.kt` give the Lambert term for any horizontally-rolled surface (nx: -1 = left silhouette edge → +1 = right). Note the curve peaks near nx = -0.6 and falls back off at nx = -1, because the lit edge still turns away from the viewer — an evenly-brightening left-to-right ramp never looks right. `rollGradient(fromX, toX, strength) { shade -> … }` is the standard way to apply it; each feature supplies only its own colour ramp (`coneFeltColor`, `beardRollColor`, `browRollColor`, `goldRollColor`, `clothRollColor`, `pantsRollColor`, `shoeRollColor`). The head sphere has its own `skinSphereColor` / `skinFalloffAt` pair, shared with the ears so they match the cheek at the junction.

Hard-won rules, all of which produced visible bugs when broken:

- **`strength` scales with the element.** The raw Lambert term runs to its extremes at the silhouette edges — right for the hat, far too much for a lens or a bow tie, where it stops reading as lit and starts reading as bruised or grubby. Small elements dial it to 0.4–0.55.
- **One object → one gradient; two objects → two.** Lapels, bow tie and sunglasses each span the whole pair with ONE gradient (shading halves separately mirrors the highlight and reads as two light sources). Trousers, shoes and buttons get one each. Shoes additionally take a global falloff across BOTH plus a lift at each shoe's leading edge.
- **Cast shadows must be clipped to what they land on.** Several features overhang the head — the brim (2.1u vs a ~1.5u head), the moustache (1.58u vs a ~1.08u jaw) — so an unclipped shadow hangs in the sky. `hatShadowSurface(u)` = head ∪ hair ∪ ears is the brim's target; leaving any part out leaves that part lit beside a shadowed face.
- **Don't stroke a specular edge onto a flat shape.** Tried and reverted three times (hat brim rim, torso edge ×3): a rim needs surface detail to sit on, and a constant-alpha stroke around a closed curve reads as a halo. The fill's roll-off carries these alone.
- **Union coincident shapes instead of abutting them.** The hat is `cone ∪ brim` and the moustache is `wing(-1) ∪ wing(+1)`, each shaded in one pass. Android does not antialias clip edges, and *any* opaque repaint beside already-shaded pixels leaves a 1px lighter seam (its antialiased edge gets ~half the shading of its neighbours). Overlapping does not cure this — only removing the internal edge does.

**`ui/components/GnomeCanvas.kt`** — Custom Compose Canvas drawing the gnome. Animations (pendulum, bounce, flash, twinkle) are synchronized to `BeatEvent` emissions from MetronomeViewModel. The `feature/metro-cosmetics` branch extends this with a `MetroItem` system: `activeItems`/`onItemTapped` params, tap-hit detection, and separate draw passes for background items, body-attached items, and head-attached items (which bob with the head group).

**`ui/components/GoldSlider.kt`** — The tuner's slider: a gold thumb whose halo brightens on every change and springs back, on a rail whose active portion is a gradient (dim at the origin, bright under the thumb) with a radial bloom where the two meet, plus an optional `markerFraction` tick for a rail with a canonical position on it (the reference pitch's 440 Hz). Extracted from `ReferencePitchCard` when the drone panel needed the same control; the drone had shipped with a stock Material slider and the two sat on one screen looking like different controls for the same gesture. Any new slider on the Tuner screen uses this. `SettingsScreen`'s `SettingsSliderRow` is deliberately left alone: it is a different screen with a plain list-row idiom, and pulling it in would be a redesign rather than a de-duplication.

**`ui/components/RaisedControl.kt`** — Shared raised-key treatment for the metronome's primary control row (steppers, play/pause, TAP). Three layers: a vertical gradient across the face (lifted top, sunk bottom), a hairline rim that is bright on the top edge and dark on the bottom (a *uniform* outline reads as a drawn border — same trap as the character's edge lighting), and nothing else. No blur, glow or drop shadow: on a background this dark a shadow is invisible, and adding one turns "raised" into "glassy". Both shading colours are **derived from the `tint` parameter**, so one component serves neutral steppers, purple TAP, and a play key that animates purple↔red with the highlight tracking it automatically. The secondary icon row is deliberately still flat (hierarchy).

**`ui/components/Seal.kt`** — The app-wide "earned and verified" mark: a scalloped rosette with a check, used wherever something is certified (Groove Check pass, achieved streak days, unlocked collection items). Three layers: `SealStyle` (all visual knobs: colors, alpha, scallop count/depth, check stroke; presets `Emblem`, `Badge`, `SealStyle.halo()`), `DrawScope.drawSeal` (the core, for seals inside a larger Canvas), and the `Seal` composable (self-contained element with slow drift rotation on the shared `SEAL_DRIFT_PERIOD_MS` clock, optional one-shot stamp `entrance`). Only the rosette rotates - the check always stays upright. New uses must adapt a `SealStyle`, never redraw the silhouette.

**`ui/components/metro_items/`** — Cosmetic item system (feature branch only).

- `MetroItem` interface: `draw(u, cx, baseY)`, `hitCenter(u)`, `hitRadius(u)`, `isBodyAttached`, `isHeadAttached`, `previewCenter(canvasW, canvasH, u, baseY)`, `previewRadius(u)`. Background items override `previewCenter`/`previewRadius` to define a tight zoom window for the unlock popup preview; body-attached wearables use `hitCenter`/`hitRadius` for the same purpose.
- `METRO_ITEM_REGISTRY` — single source of truth pairing each `MetroItem` with an `UnlockCondition`. Registry order controls background draw layering (back → front); do not reorder without checking visual correctness.
- `MetroItemTracker` — reads/writes SharedPreferences `"metro_cosmetics"`. Counters: `metronome_seconds` (incremented every 10s while playing), `games_completed` (incremented on game end), `first_launch_ms` (set once on first init). Celebration tracking: `celebrated_item_ids` — written only when the user dismisses the popup ("Sweet!" button), not when the unlock is detected. This ensures a popup bug never permanently silences an earned reward. `markCelebrated` also emits on a companion-object `celebrationDismissed: SharedFlow` — a single app-wide signal (MetronomeScreen/RhythmGameScreen/SettingsScreen each hold their own tracker instance over the same prefs file, so this is how one shared collector in `MainActivity` hears a celebration from any of them; currently used to fire the one-time notification-permission ask, see Notifications below).
- `UnlockCelebrationOverlay` — full-screen animated popup (confetti + spring card). Collected via `vm.newlyUnlocked: SharedFlow` into a `mutableStateListOf` queue in each screen; shown one at a time. `markCelebrated` is called in `onDismiss`, not during emit.
- `ItemPreviewCanvas` — 220×170dp canvas inside the overlay. Background items: translate+scale transform using `previewCenter`/`previewRadius`. Body-attached items: translate+scale using `hitCenter`/`hitRadius` with a boosted u (`size.height/5f`).

**Dev tools in `SettingsScreen`** (visible in debug builds only, gated by `BuildConfig.DEBUG`):
- Toggle cheat mode — all items unlocked while active
- Preview popup for any registry item (cycles with index button)
- Show unlock rules — scrollable dialog sorted easiest → hardest, built from registry at runtime
- Reset all progress — wipes `metronome_seconds`, `games_completed`, `first_launch_ms`, `celebrated_item_ids`
- Diagnostic logs (reworked 2026-09-15 for the same reason as the Chord Test button: months after a test was built, the dev could not tell from the buttons what to do to fill a log). They are grouped by what the developer has to do. "Mic Timing Log" and "Tuner Lock Log" are plain viewers of logs that fill themselves (`MicDiagnosticsBuffer` during any real-mic Speed Trainer session; `TunerLockLog` on every Tuner visit). "Tuner Reading Log" is the known-truth accuracy run and records only while switched on, so "Record Readings" sits beside its viewer, becomes "Stop Recording" (red) while on, and follows `TunerReadingLog.isRecording` (a `StateFlow` mirror of the `@Volatile` flag the capture thread reads) rather than a local `remember`, because `debug/tuner/TunerReadingRecordingPill.kt`, overlaid on the Tuner tab exactly like `ChordLoopRunningPill` on Chords, counts the samples live and stops the run on tap. A 9 sp caption under each row states the rule; each overlay's empty state repeats the steps. **A dev capture that happens on another tab gets a pill on that tab**: a run whose only sign of life is a red button two tabs away is a run the dev cannot tell is happening.

### Notifications

`notifications/` holds all FCM/permission logic — nothing outside this package calls a Firebase Messaging API directly:

- `MetroFcmService` — `FirebaseMessagingService` subclass; builds and posts the notification (guarded by a live `POST_NOTIFICATIONS` check, since a granted-at-send-time assumption can be stale). Overrides `onRegistered(installationId)`, not `onNewToken(token)`, to re-subscribe to the broadcast topic on every Firebase Installation re-registration — confirmed 2026-08-14 directly against the resolved `firebase-messaging` bytecode that `onNewToken` is `@Deprecated` in this version, superseded by the `onRegistered`/`onUnregistered` pair. Lint's built-in `MissingFirebaseInstanceTokenRefresh` check doesn't know about that replacement (it only recognises `onNewToken` by name) and is suppressed on the class with an explanation - do not "fix" that finding by adding `onNewToken` back.
- `NotificationChannels` — the single `"general"` channel, id must match `default_notification_channel_id` in `AndroidManifest.xml`. Created idempotently in `MetroGnomeApplication.onCreate`.
- `NotificationTopics` — broadcast model: every consenting device subscribes to one topic, `"all_users"`. No per-device tokens are collected or stored — a message sent to that topic from the Firebase Console (or a Firestore-triggered Cloud Function via the Admin SDK) reaches everyone with zero backend. `MetroGnomeApplication.onCreate` syncs subscription state with the live permission on every launch in both directions (subscribe if granted, **unsubscribe if not**, added 2026-08-14) - the unsubscribe half matters because revoking notification access happens entirely in system Settings, outside any in-app callback, so without it a device that revoked access would stay subscribed (and keep waking for pushes `MetroFcmService` already silently drops) indefinitely.
- `NotificationPermissionState` / `rememberNotificationPermissionState()` — live `POST_NOTIFICATIONS` state (always `true` pre-API 33) plus request/open-settings actions, mirroring the mic-permission pattern in `TunerScreen.kt`. Owned once in `MainActivity`, passed down to `SettingsScreen`.
- `NotificationOptInTracker` — one-shot flag for whether the contextual soft-ask (below) has ever been shown.

**Permission ask, two paths, deliberately not more:**
1. **Contextual, once ever** — fired the first time the app is opened on a 2nd distinct calendar day (`UsageDayTracker.distinctDaysCount() >= 2`, checked in `MainActivity`'s `ON_RESUME` effect - the same counter `LoyaltyDays` items use). Chosen 2026-08-14 over the original first-item-unlock trigger: that one only reached users who engaged with the cosmetic-item system, so plain-metronome users and anyone who already owned every item before this feature shipped could never be asked. Shows `ui/dialogs/NotificationOptInDialog.kt` (a plain "Enable / Not now" card) before ever spending the real system permission dialog. Never re-shown automatically after that.
2. **Self-serve, always available** — the "Notifications" row in `SettingsScreen`, always reading the live OS permission (never a stored flag — tapping while granted opens the system per-app notification screen, since an app can't revoke its own grant).

### What's New

`whatsnew/` — `AppWhatsNew.ALL` is the registry of major-version popup keys (`v3`, `v4`, `v5`, `v6`); only major versions get one, never minor/patch. `v6` (2026-09-15) introduces the Chords tab; its preview reuses the real `GuitarFretboard` at 74 dp, lighting an open Cmaj7 note by note before the name appears, and passes no degree labels because a marker that small cannot carry text. **The dismiss button is an acknowledgement, never a call to action** ("Sounds good!", like "Let's train!" before it): it only closes the card, and "Name a chord!" read as a promise to open the tab. Wiring it to navigate was considered and rejected, since every earlier popup behaves the same way. `WhatsNewTracker` persists which keys a device has confirmed and hands `MetronomeViewModel` the single next-pending key via `pendingKey()`; `MetronomeScreen` shows it via `WhatsNewOverlayDispatcher` (`ui/overlays/WhatsNewOverlay.kt`) ahead of the item-unlock queue but behind a pending practice result. **Brand-new installs never see any What's New popup** (changed 2026-08-14): the fresh-install guard in `WhatsNewTracker.init` pre-marks every key, including the latest, as already shown. Previously it left the latest key open so new installs got the last major version's popup as a de facto feature ad - removed because "NEW IN VERSION X" copy has no meaning to someone with no "before," and first cold open is the worst point in the funnel to spend a blocking modal advertising one feature out of several the user doesn't know about yet. An upgrading existing user is unaffected either way, since their prefs are never empty at that check.

### Color System

All colors are centralized in `ui/theme/Color.kt`. Do **not** add raw `Color(0xFF…)` hex literals to screen or component files — always define in the appropriate object first.

- **`GnomeColors`** — gnome character art palette (skin, hat, suit, shoes, hair, baton, FX). Used exclusively inside `GnomeCanvas.kt` draw functions. Most entries now come in lit/base/shadow triples feeding the ramps described under *Metro's lighting model* — e.g. `skin`/`skinHighlight`/`skinDark`/`skinShadow`, `hatRedLight`/`hatRed`/`hatRedDark`/`hatShadow`, `shoeLight`/`shoe`/`shoeDark`, `tieLight`/`tie`/`tieDark`. `skyRim` is the scene's ambient bounce off the night sky (named for the sky, not the hat that first needed it); `nostril` exists because `skinDark` is *lighter* than the shaded lower half of the nose and tinting with it lifted the nostrils into light dots.
- **`AppColors`** — UI chrome colors shared across screens and components: backgrounds (`background`, `surface`, `surfaceVariant`, `surfaceDeep`, `surfaceDim`, `surfaceActive`), text (`textPrimary`, `textSecondary`, `textMuted`, `textSubtle`, `textDim`, `textAccent`, `textMutedBlue`), brand (`gold`, `primaryPurple`, `mediumPurple`, `deepPurple`, `darkPurple`, `danger`), controls, stop-action colors, overlay preview gradient, confetti list, and dev-tool colors.
- **`GameColors`** — rhythm game semantics: hit quality (`good`, `almost`, `miss` — gold = `AppColors.gold` for PERFECT), note tints (`noteAmber`, `notePurple`), beat dots, mic equalizer (`eqQuiet`), hit line idle (`hitLineIdle`), range label blue (`rangeBlue`).
- **`ItemPalette`** — shared cosmetic-item drawing colors: gold trio (`goldLight`, `goldMid`, `goldDark`) used by GoldChain/GoldEarring/LuxuryWatch; wood duo (`woodBrown`, `woodLight`) used by ForestTree/TorchPost.

Item-specific colors that are unique to one file (mushroom teal, firefly glow, flower petals, moon, gem, watch strap, torch flame/grass) remain as `private val` constants inside each item's object — do not extract these unless they become shared.

**`drawable/ic_launcher_foreground.xml`** — Vector launcher icon foreground derived from GnomeCanvas.kt (u=9, cx=54, baseY=164 → 108×108 viewport). All colours match `GnomeColors`. Hat rotated 11° via `<group android:rotation="11">` matching `drawHat`. Do **not** simplify to circles/rects — the paths are intentionally accurate. It deliberately does **not** track the v5.19 shading work: a vector drawable cannot express the sweep gradient the hat uses, and at 48dp the flat fills read fine — so treat the icon as its own flat-shaded rendition, not a copy that has fallen behind.

### Typography trap

`Typography.bodyLarge` in `ui/theme/Type.kt` declares `fontSize = 16.sp` **and an absolute `lineHeight = 24.sp`**, and Material3 provides it as the ambient text style. A `Text` that overrides only `fontSize` keeps the 24.sp line height — so a 12.sp headline lays out on double-height lines. This is invisible until the string wraps, then shows as a gaping gap (found in `MicTimingNudge` 2026-08-15). **Always set `lineHeight` alongside any `fontSize` override.** Two more facts from the Chords tab's no-jump work (2026-09-16): the ambient style leaves `lineHeightStyle` at Compose's default `Trim.Both`, so on a *single* line `lineHeight` does nothing at all (the line is its natural font height, which is why the trap is invisible until a wrap); and even with `Trim.None` a `Text`'s measured height still depends on which glyphs are in it, because with font padding off Compose pads the first and last line by the glyphs' ink overhang. **A layout that must not move with its text takes its height from the density (`sp.toDp()` on a `Box`), never from the text.** **A pinned two-line caption needs all three parts of the hero's recipe** (2026-09-21): `style = PinnedLines` (literal line height, `Trim.None`), `minLines = 2, maxLines = 2`, and a `PinnedSlot` of exactly two lines. The Listening and Hear It captions shipped with only the slot and `maxLines`; on the dev's phone they were fine, on another they ellipsed to one line, because under the default trim two lines measure taller than 2 x lineHeight on some densities and a height-constrained `Text` drops a line rather than overflow. A root fix (`lineHeight = 1.5.em`, identical at 16.sp but proportional everywhere else) is deliberately not applied: it would also change text set *larger* than 16.sp, where the cramped 24.sp is currently load-bearing for layout.

For a 12.sp headline over a 10.sp subtext line (the recurring pattern in `MicTimingNudge` and `DailyBonusCta`), set `lineHeight = 16.sp` on the headline (not `12.sp` matching the font size 1:1) — tried at `12.sp` first, but on-device that read as visibly cramped once installed; `16.sp` gives a small amount of natural leading without reopening the original 24.sp gap.

### Caps labels: two tiers, on every page

A caps label is either a **card header** (12 sp, bold, 1 sp tracking, 16 sp line height; the tuner's `REFERENCE PITCH`, `CALIBRATION`, `DRONE`, every Chords card including the hero's eyebrow) or a **section label inside a card** (10 sp, bold, 1 sp tracking, 14 sp line height, `textDim`; the tuner's `AMBIENT SUPPRESSION` and `ENVIRONMENT`, the drone's `VOLUME`, `TIMBRE`, `BLEND`). Settled 2026-09-21 when the Chords page's 10 sp card headers read as pasted in beside the tuner's 12 sp ones, and the audit found `DRONE` on the wrong tier too. A new card header is 12 sp; a label under it is 10 sp; nothing is 11 sp or 9 sp because it "looked about right". Card headers are all `textDim` (2026-09-21: `CALIBRATION` and `LISTENING` were gold and `REFERENCE PITCH` was `textSubtle`; the dev asked for one colour across both pages). Gold is for a *value* that matters right now (a heard note, "Sounding"), never for a header.

### Gnotes / Points System

`points/PointsManager.getSnapshot()` never caches - every call reads live SharedPreferences via `MetroItemTracker` and recomputes the total, so any code holding a fresh `PointsManager` always sees the true current value. The trap is one layer up: `MetronomeViewModel.gnoteCount` (a `StateFlow<Int>`, the single source both the Home pill and the Rhythm page's `PointsCard` read from) is a **cached** copy of that total, and it only gets pushed a new value when something explicitly writes `_gnoteCount.value = pointsManager.getSnapshot().total` - either a `PointsBannerQueue` event (Practice/Rhythm/Tuner/ad-watch completions all post one) or `recordUsageDay()` (fired on `ON_RESUME`). Free-run metronome play credits Gnotes silently every 10s via `itemTracker.addMetronomeSeconds(10)` in `startPlayTimer()` with **no** banner event, so before 2026-08-17 the displayed total could sit stale for minutes at a time - fixed by also refreshing `_gnoteCount` inside that same 10s loop. Any future code path that credits Gnotes without going through `PointsBannerQueue` needs the same explicit refresh; recomputing `PointsManager().getSnapshot()` fresh on next screen-open is not enough; the cached `StateFlow` is what's actually on screen.

`ui/components/GnoteTickFlair.kt` - shared "Gnotes just ticked up" flair: wrap any view showing a live Gnotes total in `GnoteTickFlair(count = ...)` and it fires a small gold spark burst whenever `count` rises while it's on screen. Never fires on first composition (the value it mounts with becomes its baseline), so it composes safely alongside a caller that already animates the same value on mount (e.g. `PointsCard`'s own 0→total roll-up) - the two don't fight because the flair only reacts to a genuine rise *after* mounting, not the reveal itself. Purely additive (drawn via an overlaid same-size `Canvas`); overshoots small unclipped views (the Home pill) but is contained by any ancestor that clips (the Rhythm page's card).

`ui/components/DailyBonusCta.kt` - shared rewarded-ad "watch a clip for bonus Gnotes" row plus its confirmation (`DailyBonusConfirmDialog`), used identically by `GnotesInfoDialog` (Home) and the Rhythm page's `PointsCard`. Previously this was two independently-drifting copies of near-identical Compose code; now both call sites pass state in and get the same copy/visuals for free. The reward badge is a pulsing **ring drawn in a `Canvas` that expands past the badge's own edge** into the surrounding background, not a `Modifier.border` sitting on the badge - a same-color border directly on a solid gold fill is gold-on-gold and barely reads regardless of alpha; the ring only becomes visible by never overlapping the fill it's supposed to contrast with.

**`ads/RewardedAdManager.kt` needs its own retry, unlike `ads/AdManager.kt`'s interstitials** (fixed 2026-09-18). `AdManager.maybeShow()` calls `preload()` on every interstitial-opportunity check regardless of the previous outcome, so a failed load self-heals the next time any ad trigger fires. The rewarded slot has no equivalent: `DailyBonusCta`'s row is only tappable (`canTap = canWatchToday && adReady`) once `adLoaded` is already true, so nothing in the UI ever calls `show()` - the one path that used to retry `preload()` - while an ad is *not* loaded. Before the fix, `preload()` was called exactly once, from `MetronomeViewModel`'s init; a single `onAdFailedToLoad` (no-fill, a network blip, anything) left `adLoaded` stuck false and the row stuck on "Bonus clip loading..." for the rest of the session, on both surfaces at once since they share one `RewardedAdManager` instance - unrecoverable by navigating away and back, only by killing the app. `onAdFailedToLoad` now self-retries via `Handler.postDelayed` starting at 30s and doubling to a 5-minute cap, resetting to 30s on the next successful load - any future ad-loading class in this codebase that isn't naturally re-triggered by a UI-driven check needs this same explicit backoff, not just a `preload()` at construction time.

### Navigation

`MainActivity` hosts a `NavigationSuiteScaffold` with 5 tabs (`AppTab`): Gnome (metronome), Tuner, Chords (chord finder, added v6.0), Rhythm (game), Settings. Five is Material's maximum for a bottom bar, so labels stay one short word ("Chords", never "Chord Finder"), and there is no room for a sixth without a redesign. No `NavController` - `currentTab` is a single `rememberSaveable` enum in `MetroGnomeApp`.

**Keep screen on is one app-wide setting** (2026-09-21): `MetronomeViewModel.keepScreenOn` (prefs `keep_screen_on`), toggled by the button on the Home tab only, and applied by a single `DisposableEffect` in `MetroGnomeApp` that sets or clears `FLAG_KEEP_SCREEN_ON` on the window whatever tab is showing. Before this, Home and Tuner each applied the flag on entry and cleared it on exit and the Tuner had its own copy of the button, so the Chords tab dimmed while the other two did not and the two buttons could drift. Do not add the flag or the button to a screen; a new tab is covered already.

**FCM deep links** (added 2026-08-14): an `openTab` data key on an FCM message (value = an `AppTab` name, e.g. `"rhythm"`, case-insensitive) opens straight to that tab on notification tap. `MainActivity.EXTRA_OPEN_TAB` is the intent-extra key; `MetroFcmService` attaches it to the `PendingIntent` when it builds the notification itself, and for the case where the system posts the notification directly instead (background app, notification+data payload - see that file's kdoc), the FCM SDK copies the data payload onto the launch intent automatically, so no separate handling is needed there either. `MainActivity` is `launchMode="singleTop"` specifically so a tapped notification while the app is already running reuses the instance via `onNewIntent` rather than tearing down and recreating every ViewModel (which would kill an in-progress metronome/tuner/rhythm session). `MetroGnomeApp(openTab, onOpenTabConsumed)` applies it via a `LaunchedEffect` and immediately consumes it (resets to null) so a second notification tap for the same tab still re-triggers. **All tab switching, including this deep link, goes through one `switchTab(tab)` local function** in `MetroGnomeApp` (extracted 2026-08-14 during a pre-publish audit) - the deep-link `LaunchedEffect` originally set `currentTab` directly, silently skipping the RHYTHM-leave guard (`rhythmVm.stopGame()`) and the Tuner/Settings review-prompt trigger that the nav bar's `onClick` already had. Any future tab-switch side effect belongs in `switchTab`, not duplicated at each call site.

### Website (`landing/`, master branch only)

metrognome.co.za is a static site under `landing/` that exists **only on `master`** (Vercel deploys from there; Root Directory = landing, config in `landing/vercel.json`). The SEO log (`seo_context.md`, `seo_reports/`) lives on master too. Never read or edit any of it from a version branch: the copy there is a stale pre-redesign snapshot. Read `seo_context.md` before any SEO or GSC work; it closes questions that otherwise get re-investigated.

**The homepage tracks the app's feature set** (last synced 2026-09-16 for v6): six showcase screenshots in tab order (Metronome, Tuner, Chord Finder, Rhythm Game, Gnotes, Collectibles; 600x1268 PNGs resized straight from the Play Store image creator's framed captures, which are the same aspect ratio), and a "Far more than a metronome" section with **nine feature cards in a 3x3** (metronome, tuner, drone, chord finder, rhythm game, speed trainer, Groove Check, practice timer, Gnotes & collectibles; the grid is six tracks with cards spanning two, so a short last row centres itself if a card is added or removed). A new tab or hero-level feature needs the same places touched every time: hero subhead, the meta/OG/Twitter descriptions, both JSON-LD blocks (`featureList` and `FAQPage`), the visible FAQ, a card, and the "No subscription. Ever." promise. **Never describe Gnotes or items as "rewards"** on the site or in listing copy: the dev runs another app that gives away real weekly airtime rewards, and the word reads as a giveaway; say Gnotes earned by practising that unlock items for Metro. The only legitimate use is the ad-free days for hitting the daily Gnote goal. Pre-publish checks: both JSON-LD blocks parse, no em or en dashes, every internal link resolves, sitemap `lastmod` bumped, then `python landing/indexnow.py /` once Vercel is live.

**Five browser tools port app code, so changing the app side means changing the tool on master too:**
- `landing/tools/chord-finder.html` ports `theory/Chord.kt` verbatim in JS (the dictionary, degree spelling, spelled octaves, bass-decides ranking, the (no 5) rule) as one block between `var NAMES` and `/* state */`. A Node harness re-ran all 216 expectations of `ChordTheoryTest` against the port when it shipped (2026-09-15); if `Chord.kt` changes, port the change and re-run that check, then regenerate the embed. Its "hear it" uses Web Audio with the same partials and envelope as `ChordTimbre.PLAYBACK` and the QUICK pace, so the phone and the browser sound alike. The companion post is `landing/blog/what-is-a-chord-how-chords-work.html`.
- `landing/tools/odd-meter-practice-tool.html` ports `theory/Meter.kt` (`classify`/`beatGrouping`/`label`/`description`) verbatim in JS. Eight per-meter guide pages under `landing/tools/time-signatures/` embed it via `?ts=7-8&bpm=108`.
- `landing/tools/tuning-drone.html` ports `audio/drone/DroneVoice.kt`: the Pure and Warm timbres, Root and Octave blends, strand detunes, LFO rates, equal-power pan and power-summed normalisation to `TARGET_RMS`. Reed, String and Fifth are deliberately **not** ported (they are the paid voices); the page shows them as disabled chips. Deep links `?note=Cs&octave=3&timbre=warm&blend=octave&ref=440`; `landing/tools/shruti-box-online.html` embeds it pre-set to kali 1.
- `landing/tools/tap-tempo-bpm-finder.html` carries `MetronomeScreen.kt`'s `tempoLabel()` table; `landing/tools/rhythm-timing-test.html` uses `GrooveScorer.kt`'s jitter constants.

**The tools index is a screenshot catalogue** (2026-09-26): `landing/tools/index.html` shows one card per tool with a real capture of it in use (`landing/tools/img/*.webp`, 640 px square), a plain name as the title, and an "Open the X" button; the long search titles stay on the tool pages. A tool whose UI changes visibly needs its card re-captured, and a new tool needs a card. Captures come from headless Chrome loading the live tool with a deep link (or a hook into its finish function) and cropping the `.tool-panel` below Metro; headless Chrome cannot render narrower than about 500 px, so phone-width checks go through a 390 px iframe. **Testing a browser tool's timing:** run the page's own `<script>` in Node on a virtual clock with a stub DOM and inject events at exact times. The Chrome extension's tabs report `visibilityState: hidden` and batch timers to about 500 ms, so live timing measured through it is meaningless. The rhythm test scores taps on `pointerdown` using the event's `timeStamp`, never on `click` (which fires on release and would score finger-rest time as the player's spread); starting a run stays on `click`, since only a touch release may unlock audio.

**Embed widgets are generated, never hand-edited.** `landing/embed/build-embeds.sh` extracts each tool's `.tool-panel` section and its bare `  <script>` block into `landing/embed/*.html` (noindex, canonical to the tool, credit link inside the panel). After editing any file in `landing/tools/`, run it and commit the output; a stale widget is already live on other people's sites. Adding a tool means one more `emit` line. The tool page's panel must not contain a nested `</section>` and its script must sit at exactly two-space indentation, or the extraction misses it.

**After a publish goes live:** `python landing/indexnow.py` (all sitemap URLs) or with paths, for Bing/DuckDuckGo/Copilot. Google ignores IndexNow; the dev requests indexing in GSC by hand. Site rules that are checked before every publish: no em or en dashes anywhere, every JSON-LD block parses, every internal link resolves, every page in the sitemap exists.

### Dependencies

Versions managed via `gradle/libs.versions.toml`. Key: Compose BOM 2026.09.00, AGP 9.3.1, Kotlin 2.4.20, coroutines 1.11.0, Google Play Ads 25.4.0. Note: `implementation(platform(bom))` and `androidTestImplementation(platform(bom))` are both required (BOMs pin per configuration) — the IDE's duplicate-dependency warning on this is a false positive.

### Release Signing

Release builds require `keystore.properties` at the project root referencing `metrognome-release.jks`.

### Permissions

Four are declared in `AndroidManifest.xml`:

- `INTERNET` — Google AdMob ads, Firebase.
- `RECORD_AUDIO` — used by **four** features, not just the rhythm game: the tuner (`audio/tuner/Tuner.kt`), the rhythm game's clap input, Groove Check during Practice and Speed Trainer (`audio/rhythm/RhythmDetector.kt`), and the Groove Check self-test (`audio/selftest/`). Requested at runtime, and every request path goes through `ui/components/MicPermissionRecovery.kt` rather than its own launcher (see the Groove Check permission recovery section above for why). The drone is the one audio feature that deliberately gives the mic *up* rather than needing it.
- `VIBRATE` — `haptics/HapticEngine.kt`.
- `POST_NOTIFICATIONS` — FCM broadcast pushes, API 33+. Requested through `notifications/NotificationPermissionState.kt`; see the Notifications section for the two ask paths.
