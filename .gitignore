*.iml
.gradle
.kotlin
/local.properties
/.idea/caches
/.idea/libraries
/.idea/modules.xml
/.idea/workspace.xml
/.idea/navEditor.xml
/.idea/assetWizardSettings.xml
# Machine-specific: holds the local JDK name (temurin-21 vs jbr-21), which
# flips whenever Android Studio picks a different JDK and shows up as a
# spurious diff on every branch switch.
/.idea/misc.xml
.DS_Store
/build
/captures
.externalNativeBuild
.cxx
local.properties
keystore.properties
*.jks
*.keystore
google-services.json
*.aab
*.apk

# Claude Code per-machine local settings (permissions etc.); the shared .claude/settings.json,
# if added, stays tracked.
.claude/settings.local.json

# Local-only tuner test-tone harness (generator + large WAV + manifest); regenerated on demand
/tools/tuner_test_tones/

# Release build outputs (the AAB is already covered by *.aab; mapping.txt is ~90 MB
# and Crashlytics already receives it via injectCrashlyticsMappingFileId).
/app/release/

# Play Store listing copy, kept locally as a scratch file for editing sessions.
# The authoritative copy is the Play Console itself; see seo_context.md for the log.
PSL.txt
